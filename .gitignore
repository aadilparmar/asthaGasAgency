node_modules/
.next/
.env
.env.local
.env.production
*.db
*.db-journal

# Secrets used by scripts/ (must never be committed)
scripts/.admin-password.tmp
scripts/*.tmp

# Build artifacts
tsconfig.tsbuildinfo

# Tooling (per-user)
.claude/

# Source spreadsheets — contain real payroll data, reference only
*.xlsx
